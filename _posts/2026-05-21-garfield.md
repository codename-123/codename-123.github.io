---
title: "Garfield (Hard)"
date: 2026-05-21
layout: single
excerpt: "Garfield는 Hard 난이도의 Windows 머신으로, 초기에는 제공된 j.arbuckle 계정으로 도메인과 SMB/SYSVOL 을 열람하며 시작된다. bloodyAD 로 쓰기 가능한 객체를 확인한 결과 l.wilson 및 l.wilson_adm 계정에 대한 제어 가능성을 발견하고, SYSVOL의 로그인 스크립트 경로에 악성 배치 파일을 업로드한 뒤 l.wilson 의 scriptPath를 조작해 리버스 셸을 획득한다. l.wilson 권한으로 l.wilson_adm 의 비밀번호를 재설정하고 WinRM을 통해 관리자 계정으로 수평 이동한다. 내부 정찰 과정에서 RODC01이 별도 내부망에 존재하는 것을 확인하였고, l.wilson_adm 이 RODC 관련 권한과 SeMachineAccountPrivilege 를 가진 점을 이용해 RBCD$ 컴퓨터 계정을 생성한 후, RBCD를 설정하여 Administrator 로 가장한 HTTP/RODC01.garfield.htb 서비스 티켓을 발급받고, Ligolo로 내부망 터널을 구성하여 RODC01에 Administrator 권한으로 접근한다. RODC01에서는 SeBackupPrivilege 를 이용해 ntds.dit 와 SYSTEM 하이브를 덤프하고, secretsdump 를 통해 RODC 전용 krbtgt_8245 계정의 NTLM 해시를 획득한다. 마지막으로 RODC01 객체의 PRP를 수정한 뒤, Rubeus를 이용해 RODC Golden Ticket 및 Key List 공격을 수행하여 도메인 Administrator 의 NTLM 해시를 획득한다. 최종적으로 획득한 Administrator 해시로 WinRM에 접속하여 Domain Administrator 권한을 획득한다."
author_profile: true
toc: true
toc_label: "Garfield"
toc_icon: "book"
toc_sticky: true
header:
  teaser: /assets/htb-windows/garfield/garfield.png
  teaser_home_page: true
categories: [hackthebox-windows]
tags: [active]

---

![Garfield](/assets/htb-windows/garfield/garfield.png)

**Garfield**는 Hard 난이도의 Windows 머신으로, 초기에는 제공된 `j.arbuckle` 계정으로 도메인과 `SMB/SYSVOL` 을 열람하며 시작된다. 

`bloodyAD` 로 쓰기 가능한 객체를 확인한 결과 `l.wilson` 및 `l.wilson_adm` 계정에 대한 제어 가능성을 발견하고, SYSVOL의 로그인 스크립트 경로에 악성 배치 파일을 업로드한 뒤 `l.wilson` 의 **scriptPath를 조작해 리버스 셸을 획득**한다. 

`l.wilson` 권한으로 `l.wilson_adm` 의 비밀번호를 재설정하고 WinRM을 통해 관리자 계정으로 수평 이동한다. 

내부 정찰 과정에서 RODC01이 별도 내부망에 존재하는 것을 확인하였고, `l.wilson_adm` 이 RODC 관련 권한과 **SeMachineAccountPrivilege** 를 가진 점을 이용해 `RBCD$` 컴퓨터 계정을 생성한 후, RBCD를 설정하여 `Administrator` 로 가장한 `HTTP/RODC01.garfield.htb` 서비스 티켓을 발급받고, Ligolo로 내부망 터널을 구성하여 RODC01에 `Administrator` 권한으로 접근한다.

RODC01에서는 **SeBackupPrivilege** 를 이용해 `ntds.dit` 와 `SYSTEM` 하이브를 덤프하고, `secretsdump` 를 통해 RODC 전용 `krbtgt_8245` 계정의 NTLM 해시를 획득한다.

마지막으로 RODC01 객체의 PRP를 수정한 뒤, Rubeus를 이용해 **RODC Golden Ticket 및 Key List 공격**을 수행하여 도메인 `Administrator` 의 NTLM 해시를 획득한다.

최종적으로 획득한 **Administrator 해시로 WinRM에 접속**하여 Domain Administrator 권한을 획득한다.

---

# Enumeration

## Portscan

먼저 대상 Host(`10.129.244.207`)에 대해 기본 스크립트와 서비스 버전 탐지를 수행하였다:

```bash
$ nmap -sC -sV 10.129.244.207

Starting Nmap 7.95 ( https://nmap.org ) at 2026-05-21 08:21 EDT
Nmap scan report for garfield.htb (10.129.244.207)
Host is up (0.20s latency).
Not shown: 986 filtered tcp ports (no-response)
PORT     STATE SERVICE       VERSION
53/tcp   open  domain        Simple DNS Plus
88/tcp   open  kerberos-sec  Microsoft Windows Kerberos (server time: 2026-05-21 20:21:10Z)
135/tcp  open  msrpc         Microsoft Windows RPC
139/tcp  open  netbios-ssn   Microsoft Windows netbios-ssn
389/tcp  open  ldap          Microsoft Windows Active Directory LDAP (Domain: garfield.htb0., Site: Default-First-Site-Name)
445/tcp  open  microsoft-ds?
464/tcp  open  kpasswd5?
593/tcp  open  ncacn_http    Microsoft Windows RPC over HTTP 1.0
636/tcp  open  tcpwrapped
2179/tcp open  vmrdp?
3268/tcp open  ldap          Microsoft Windows Active Directory LDAP (Domain: garfield.htb0., Site: Default-First-Site-Name)
3269/tcp open  tcpwrapped
3389/tcp open  ms-wbt-server Microsoft Terminal Services
| ssl-cert: Subject: commonName=DC01.garfield.htb
| Not valid before: 2026-02-13T01:10:36
|_Not valid after:  2026-08-15T01:10:36
|_ssl-date: 2026-05-21T20:22:17+00:00; +7h59m51s from scanner time.
| rdp-ntlm-info: 
|   Target_Name: GARFIELD
|   NetBIOS_Domain_Name: GARFIELD
|   NetBIOS_Computer_Name: DC01
|   DNS_Domain_Name: garfield.htb
|   DNS_Computer_Name: DC01.garfield.htb
|   DNS_Tree_Name: garfield.htb
|   Product_Version: 10.0.17763
|_  System_Time: 2026-05-21T20:21:36+00:00
5985/tcp open  http          Microsoft HTTPAPI httpd 2.0 (SSDP/UPnP)
|_http-server-header: Microsoft-HTTPAPI/2.0
|_http-title: Not Found
Service Info: Host: DC01; OS: Windows; CPE: cpe:/o:microsoft:windows

Host script results:
| smb2-security-mode: 
|   3:1:1: 
|_    Message signing enabled and required
|_clock-skew: mean: 7h59m50s, deviation: 0s, median: 7h59m49s
| smb2-time: 
|   date: 2026-05-21T20:21:38
|_  start_date: N/A

Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
Nmap done: 1 IP address (1 host up) scanned in 89.75 seconds
```

스캔 결과, Kerberos(`88`), LDAP(`389`, `636`, `3268`, `3269`), SMB(`445`), WinRM(`5985`), DNS(`53`), RPC(`135`, `593`), RDP(`3389`) 등의 서비스가 활성화되어 있었다.

또한 LDAP 배너 정보를 통해 도메인은 `garfield.htb`, 호스트는 `DC01.garfield.htb` 임을 확인하였다.
